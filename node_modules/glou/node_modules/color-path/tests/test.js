'use strict';

var path = require('path');

var colorPath = require('..');

console.log(colorPath.colorize(path.join(process.cwd(), 'tests/test.js')));
console.log(colorPath.colorize(path.join(__dirname, '../package.json')));
console.log(colorPath.colorize('package.json'));

console.log(colorPath.colorize(path.join(process.cwd(), 'tests/test.js'), {colors: false}));
console.log(colorPath.colorize(path.join(__dirname, '../package.json'), {colors: false}));
console.log(colorPath.colorize('package.json', {colors: false}));
